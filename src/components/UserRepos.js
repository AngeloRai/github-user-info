import React from 'react'

function UserRepos(repositories) {
    return (
        <div>
            {/* {repositories.map(repo => {
                return (
                   <div>{repo.name} </div> 
                )
            })} */}
        </div>
    )
}

export default UserRepos
